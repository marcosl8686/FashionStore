<nav class="navbar navbar-default jolie-navbar">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand jolie-brand" href="#">JOLIE</a>
        </div>

        <div class="collapse navbar-collapse" id="myNavbar">

            <ul class="nav navbar-nav navbar-center">
                <li class="links-center"><a href="/">Home</a></li>
                {{#unless isAuthenticated}}
                    <li class="links-center"><a href="#about">About Us</a></li>
                    <li class="links-center"><a href="#contact">Contact</a></li>
                {{/unless}}
                {{#if isAuthenticated}}
                    <li class="links-center"><a href="/product/members">Products</a></li>
                {{/if}}
            </ul>

            <ul class="nav navbar-nav navbar-right">
                {{#if isAuthenticated}}
                    <li><a href="/logout/sign-out" class="page-scroll" data-toggle="modal">Logout</a></li>
                    <li><a href="/checkout/total" class="btn shoppingbasket"><i class="fa fa-shopping-cart fa-2x"
                                                                                aria-hidden="true"></i></a></li>
                {{else}}
                    <li><a href="#" data-toggle="modal" data-target="#myModal2"><span
                            class="glyphicon glyphicon-log-in"></span> Login</a></li>
                {{/if}}
                {{#if account.admin}}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                           aria-expanded="false">Clients<span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="activeClients" class="active-clients">Active Clients</a></li>
                            <li><a href="pendingClients" class="pending-clients">Pending Clients</a></li>
                        </ul>
                    </li>
                {{/if}}
            </ul>
        </div>

    </div><!-- /.container-fluid -->
</nav>

{{#unless isAuthenticated}}
    <section class="banner">
        <p>Create an account to check out our latest LOOKBOOK!<a class="create-btn jolie-btns" href="/signup">Sign
            Up</a></p>
    </section>
{{/unless}}